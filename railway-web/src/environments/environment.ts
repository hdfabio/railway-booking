/**
 * Leave apiBaseUrl empty when using `ng serve` with proxy.conf.json (recommended).
 * Set to `http://localhost:5000` only if you run the UI without the dev proxy.
 */
export const environment = {
  apiBaseUrl: ''
};
